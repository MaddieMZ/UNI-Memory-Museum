#version 330 core //Definimos la version del lenguaje de sombreado
out vec4 FragColor; //Definimos una variable de salida para que la figura se renderice con color
void main()
{
FragColor = vec4(0.8f, 0.3f, 0.02f, 1.0f); //Definimos el color que va a tener la figura
}
